apiVersion: v1
kind: Service
metadata:
  name: http-service
  labels:
    app: httpserver
spec:
  selector:
    app: httpserver
  ports:
  - name: httpserver-port
    protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP


 
